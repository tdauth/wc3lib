language: cpp
compiler:
- clang
os:
- linux
- windows
script:
  - mkdir build
  - cd build
  - cmake .. && make && cpack .
addons:
  apt:
    sources:
      - boost-latest
      - debian-sid
    packages:
      - cmake
      - boost-defaults
      - qt5-default